import React, {useState} from "react";
import './index.less';
import {Layout} from 'antd';
import Header from "../Header";

function Home() {
    return (
        <Layout>
            <Header/>
        </Layout>
    );
}

export default Home;
